<?php

namespace Database\Seeders;

use App\Models\Post;
use App\Models\PostTag;
use Carbon\Carbon;
use Illuminate\Database\Seeder;

class PostTagSeeder extends Seeder
{
    /**
     * Run the database seeds.
     */
    public function run(): void
    {
//        $now = Carbon::now()->toDateTimeString();
//        PostTag::create(['id' => 1, 'post_id' => 1, 'tag_id' => 1, 'created_at'=>$now]);
//        PostTag::create(['id' => 2, 'post_id' => 1, 'tag_id' => 2, 'created_at'=>$now]);
//        PostTag::create(['id' => 3, 'post_id' => 2, 'tag_id' => 2, 'created_at'=>$now]);
//        PostTag::create(['id' => 4, 'post_id' => 3, 'tag_id' => 1, 'created_at'=>$now]);
//        PostTag::create(['id' => 5, 'post_id' => 4, 'tag_id' => 3, 'created_at'=>$now]);

//        Post::find(1)->tags()->attach([1, 2]);
//        Post::find(2)->tags()->attach([1, 2, 3]);
//        Post::find(3)->tags()->attach([2]);
//        Post::find(4)->tags()->attach([3]);
    }
}
